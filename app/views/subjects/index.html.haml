- content_for :title, '課題'

.row
  - @subjects.group_by(&:term_id).each do |year, subjects|
    %section.col-lg-6.mb-5
      %header
        %h2= year

      .list-group
        = render partial: 'subjects/subject', collection: subjects, cached: true
